# Development Log — 2026-07-24

## Milestone 4: Public Frontend

### Session Start

Synced `main` (`git checkout main && git pull`), confirmed Milestones 1,
2A, 2B, 3A, 3B, and 3C are all merged (`git log --merges` shows five
`Merge pull request` commits plus PR #7's squash merge for 3C), working
tree clean, `docker compose ps` showed the database already healthy, and
`./mvnw clean verify` passed 147/147 on `main` before any new work began.
Created `milestone/04-public-frontend` from `main`.

Read the live backend's `/v3/api-docs` directly (`curl` + `python3 -m
json.tool`) before writing any frontend code, per the task instructions —
confirmed the real `ResourceResponse`/`CategoryResponse` shapes, that
pagination is 0-based, and that `sort` is allowlisted to `name`/`createdAt`.
This is also what caught that the resource schema has no `accessibility` or
`lastVerifiedAt` field, contrary to what the task brief's UI requirements
implied — see the milestone document's "A Note on Scope."

Also read `frontend/AGENTS.md`'s warning about this Next.js version (16.x)
differing from training data, and read the relevant bundled docs under
`frontend/node_modules/next/dist/docs/` before writing App Router code —
confirmed `params`/`searchParams` are Promises (as expected from recent
Next.js versions) and that `error.tsx` now receives an `unstable_retry`
callback in addition to the older `reset`.

### Stray Duplicate Files

Found and removed ~13 harmless `NAME 2.EXT` duplicate files (Java sources,
markdown docs) left over from a prior session's interrupted `git pull` —
confirmed byte-identical to their real counterparts via `diff -q` before
deleting, and confirmed they were untracked (not referenced by git) first.

### Backend CORS

Added `com.hfxconnect.common.config.WebCorsConfig` (`/api/v1/**`, allowed
origins from `CORS_ALLOWED_ORIGINS`, default `http://localhost:3000`) and
`CorsConfigurationIntegrationTest` (2 tests: allowed origin's preflight
succeeds; an arbitrary unconfigured origin's preflight gets no
`Access-Control-Allow-Origin` header). See
[ADR-006](../decisions/ADR-006-frontend-backend-connectivity.md) for the
direct-CORS-vs-proxy decision.

### Jest Configuration Bug (Real, Pre-Existing)

`npm test` failed immediately with `nextJest is not a function` — the
TypeScript config loader this Jest version uses resolves `next/jest.js`'s
CommonJS default export incorrectly on this Node runtime (confirmed
directly: a plain Node `require('next/jest.js')` returns the real function;
the same import inside `jest.config.ts`, as loaded by `jest-config`'s TS
resolution, did not). Converting `jest.config.ts` → `jest.config.mjs` with a
native ESM `import` (verified directly against the installed package before
committing to this fix) resolved it. This is unrelated to the disk-space
issue below and would have needed fixing regardless of environment.

### Disk Space Environment Problem

Partway through implementation, `npm test`, `tsc --noEmit`, and eventually
even `cat` of small tracked files began hanging indefinitely. `df -h`
showed the internal disk within ~1.6GB of its 228GB capacity. Diagnosed via:
elimination (confirmed `/tmp` reads/writes stayed fast, isolating the
problem to the project's own disk location), `lsof`/`sample` on a hung
`jest` process (showed near-zero CPU accumulation over minutes — genuinely
blocked, not computing), and reproducing the same hang with `tsc --noEmit`
(ruling out a Jest-specific bug).

Flagged this to the project owner rather than attempting to free disk space
unilaterally (freeing space safely requires knowing what's safe to delete
outside this project, which isn't this session's call to make). Owner
connected an external SSD (`/Volumes/PortableSSD`, 861GB free) and asked
for the project to be transferred there and the original freed afterward.

Transfer approach (first attempt — a single `rsync` of everything including
`node_modules`/`.git` — was abandoned after ~20 minutes at only ~680 files
copied, confirming the degraded disk made a full recursive copy
impractically slow):

1. `rsync` of source files only (excluding `node_modules`, `.next`,
   `coverage`, `target`, `.git`) — 194 files, ~1.2MB, completed in seconds.
2. Separate `rsync` of `.git` (536KB, 461 loose objects) — needed two
   passes (the first appeared to finish per its process exit but left the
   destination with only 67/461 objects; a second, identical `rsync`
   invocation copied the rest) — verified with `git fsck --full` (clean)
   and `git status`/`git log` on the SSD copy afterward.
3. One more incremental source-only `rsync` to catch up files written
   between steps 1 and later implementation work.
4. `diff -rq` (excluding the same build-artifact directories) between the
   original and the SSD copy — confirmed the SSD copy was a strict superset
   (newer content only, nothing missing) before treating it as canonical.
5. Fresh `npm install` (678 packages in 5 seconds — versus indefinite
   hangs on the original disk, confirming the root cause was disk I/O, not
   this project's dependencies or configuration) and `./mvnw` (regenerates
   `target/` from scratch) on the SSD copy.

All further implementation, testing, and verification for this milestone
happened from `/Volumes/PortableSSD/Projects/HFX Project`. The original
Desktop copy is removed at the end of this milestone, per the project
owner's request, once the SSD copy's branch is pushed and verified on
GitHub.

### Implementation

Built, in order: the typed API client and Zod validation layer
(`lib/api/`, `lib/validation/schemas.ts`) verified end-to-end against the
live backend before any UI was written; the TanStack Query
prefetch/hydration foundation (`lib/query/`); the homepage
(`app/page.tsx`) with independent per-section error handling; the
`/resources` list (`app/resources/`, `components/resources/`) with URL-state
filtering/sorting/pagination and a no-JavaScript-safe filter form; the
resource detail page (`app/resources/[slug]/`) with `notFound()` handling
and safe optional-field omission; the mobile navigation disclosure pattern.

### Testing

Wrote 82 tests across 18 new/updated suites. Running the full suite against
the SSD copy surfaced two real defects (not test-writing artifacts):

1. `ResourceListView` rendered two identical "Reset filters" links when a
   category filter produced zero results.
2. A test's own regex (`/example\.org/`) ambiguously matched both a mailto
   link and a website link.

Both fixed; full suite (82/82) and full backend regression (149/149, run
fresh from the SSD copy) pass.

### Manual Verification

No browser-automation tool was available in this environment. Verified via
`curl` against the running `npm run dev` server and backend: homepage/list/
detail SSR content, category filtering, invalid-parameter fallback
behavior, out-of-range pagination, unknown-slug not-found rendering, a real
backend stop/restart cycle (frontend error state on the homepage's
independent sections; recovery confirmed immediately after restart — no
stale caching, since all fetches use `cache: "no-store"`), and a real CORS
preflight/`GET` request with an `Origin` header. See the milestone
document's Manual Verification section for the full, itemized list,
including what specifically could not be verified without a graphical
browser (the `/resources` page's client-side, post-hydration error-state
transition — covered instead by an automated test).

### Test Data

No new fabricated resource/category records were added this session. The
data used throughout verification (category "Study Spaces 3C Test",
resource "Halifax Central Library") was created through the real API during
Milestone 3C's own manual verification and reused here, since this
milestone's purpose is displaying real API data, not generating new sample
content.
